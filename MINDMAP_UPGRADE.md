# 思维导图组件升级指南

## 📋 升级说明

由于 MindElixir 库存在布局渲染问题（根节点不显示），我们已经将思维导图库更换为更稳定可靠的 **jsMind**。

## 🔄 升级步骤

### 1. 安装新依赖

```bash
cd D:\Project\xian-AIKnowledgeBase\AIKnowledgeBaseFrontend
npm install
```

这将安装 `jsmind@^0.8.5` 并移除 `mind-elixir`。

### 2. 更新组件引用

在 `src/views/KnowledgeContent.vue` 中，将：

```javascript
import MindMapEditor from '@/components/MindMapEditor.vue'
```

改为：

```javascript
import MindMapEditor from '@/components/MindMapEditorNew.vue'
```

### 3. 重启开发服务器

```bash
# 停止当前服务器 (Ctrl + C)
npm run dev
```

### 4. 测试新组件

1. 创建新思维导图
2. 查看是否正常显示
3. 测试编辑功能

## ✨ jsMind 的优势

| 特性 | MindElixir | jsMind |
|------|-----------|--------|
| 稳定性 | ⚠️ 布局问题 | ✅ 非常稳定 |
| 文档 | 一般 | ✅ 完善 |
| 社区 | 较小 | ✅ 活跃 |
| 性能 | 一般 | ✅ 优秀 |
| 主题 | 有限 | ✅ 丰富 |
| 导出 | 有限 | ✅ 多格式 |

## 🎯 新功能

### 1. 更好的布局
- ✅ 根节点始终可见
- ✅ 自动布局优化
- ✅ 平滑的缩放

### 2. 更强的编辑
- ✅ Tab - 添加子节点
- ✅ Enter - 添加同级节点
- ✅ F2 - 编辑节点
- ✅ Delete - 删除节点
- ✅ 拖拽移动节点

### 3. 更多主题
- primary (默认)
- greensea
- nephrite
- belizehole
- wisteria
- asphalt
- orange
- pumpkin
- pomegranate
- clouds
- asbestos

## 📊 数据格式

jsMind 使用标准的思维导图数据格式，与现有数据库结构完全兼容。

### 存储格式（数据库）
```json
{
  "nodeData": {
    "id": "root",
    "topic": "标题",
    "root": true,
    "children": [
      {
        "id": "child1",
        "topic": "子主题1",
        "children": []
      }
    ]
  },
  "linkData": {}
}
```

### jsMind 内部格式
```json
{
  "meta": {
    "name": "思维导图",
    "version": "1.0"
  },
  "format": "node_tree",
  "data": {
    "id": "root",
    "topic": "标题",
    "children": [...]
  }
}
```

组件会自动在两种格式之间转换。

## 🔧 自定义配置

如需修改主题或布局，编辑 `MindMapEditorNew.vue` 中的 options：

```javascript
const options = {
  container: mindmapContainer.value,
  editable: true,
  theme: 'primary',  // 修改主题
  mode: 'full',
  view: {
    hmargin: 100,    // 水平边距
    vmargin: 50,     // 垂直边距
    line_width: 2,   // 连线宽度
    line_color: '#42b983'  // 连线颜色
  },
  layout: {
    hspace: 30,      // 水平间距
    vspace: 20,      // 垂直间距
    pspace: 13       // 节点内边距
  }
}
```

## 📝 注意事项

1. **数据兼容性**: 现有数据完全兼容，无需迁移
2. **图片和备注**: 功能保持不变
3. **快捷键**: 略有不同，请查看帮助对话框
4. **性能**: jsMind 性能更好，支持更多节点

## 🐛 问题排查

### 如果思维导图不显示

1. 检查控制台是否有错误
2. 确认 jsmind 已正确安装
3. 检查容器尺寸是否正确
4. 尝试刷新页面

### 如果数据加载失败

1. 检查数据格式是否正确
2. 查看控制台日志
3. 确认后端返回的数据结构

## 📚 参考资料

- [jsMind 官方文档](https://hizzgdev.github.io/jsmind/)
- [jsMind GitHub](https://github.com/hizzgdev/jsmind)
- [在线演示](https://hizzgdev.github.io/jsmind/example/1_basic.html)

## ✅ 验证清单

升级完成后，请验证以下功能：

- [ ] 创建新思维导图
- [ ] 根节点正确显示
- [ ] 子节点正确显示
- [ ] 连接线正确显示
- [ ] 可以编辑节点
- [ ] 可以添加/删除节点
- [ ] 可以添加图片
- [ ] 可以添加备注
- [ ] 可以保存
- [ ] 可以缩放

---

**升级完成后，旧的 `MindMapEditor.vue` 可以保留作为备份，或者删除。**
